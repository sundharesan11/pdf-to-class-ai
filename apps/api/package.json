{
  "name": "@eduagent/api",
  "version": "0.0.0",
  "private": true,
  "description": "Python backend for EduAgent platform",
  "scripts": {
    "dev": "cd .. && echo 'Run: python -m uvicorn src.main:app --reload --port 8000'",
    "build": "echo 'This is a Python project, no build needed'",
    "start": "echo 'Run: python -m uvicorn src.main:app --host 0.0.0.0 --port 8000'"
  }
}
